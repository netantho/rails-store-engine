<% content_for :menu do %>
	<div class="navbar navbar-inverse">
	  <div class="navbar-inner">
	    <a class="brand" href="#">Admin StoreEngine</a>
	    <ul class="nav">
	      <li><a href="<%= root_path %>">Website</a></li>
	      <li><a href="<%= admin_products_path %>">Products</a></li>
	      <li><a href="<%= admin_categories_path %>">Categories</a></li>
	    </ul>
	    <ul class="nav pull-right">
			<li class="dropdown">
				<a href="#" class="dropdown-toggle" data-toggle="dropdown">
					Logged as
					<% if current_user.display_name.nil? %>
						<%= current_user.full_name %>
					<% else %>
						<%= current_user.display_name %>
					<% end %>
					<b class="caret"></b>
				</a>
				<ul class="dropdown-menu">
					<li><a href="<%= logout_path %>">Logout</a></li>
				</ul>
			</li>
	    </ul>
	  </div>
	</div>
<% end %>
<%= render :template => 'layouts/application' %>